class Mahasiswa {
  String _nama;
  int _nrp;

  getNama(String nama) {
    _nama = nama;
  }

  void set nama(String nama) {
    _nama = nama;
  }

  getNrp(int nrp) {
    _nrp = nrp;
  }

  void set nrp(int nrp) {
    _nrp = nrp;
  }
}
